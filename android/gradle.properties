org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# Keep classic KGP off-the-shelf flow: AGP 8.x + plugins that still apply
# `org.jetbrains.kotlin.android`. AGP 9 + built-in Kotlin doesn't yet have
# universal plugin support (flutter_foreground_task, flutter_litert_lm,
# objectbox_flutter_libs, shared_preferences_android still apply KGP
# unconditionally and break with AGP 9 + builtInKotlin=true).
android.builtInKotlin=false
